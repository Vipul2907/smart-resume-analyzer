<!doctype html><html lang="en" class="dark"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Verify your email · SmartCV</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="min-h-screen bg-ink text-zinc-100"><main class="grid min-h-screen place-items-center p-5"><section class="card glow w-full max-w-md p-7 text-center"><div class="mx-auto grid h-13 w-13 place-items-center rounded-2xl bg-cyan-400/10 text-2xl text-cyan-300">✉</div><p class="eyebrow mt-6">One last step</p><h1 class="mt-3 text-2xl font-semibold">Verify your email.</h1><p class="mt-3 text-sm leading-6 text-slate-400">We sent a secure verification link to <span class="text-slate-200">{{ auth()->user()->email }}</span>. Open it to activate your free workspace.</p>@if(session('status'))<p class="mt-5 rounded-xl bg-emerald-400/10 p-3 text-sm text-emerald-200">{{ session('status') }}</p>@endif<form method="POST" action="{{ route('verification.send') }}" class="mt-6">@csrf<button class="btn btn-secondary w-full">Send a new verification link</button></form><form method="POST" action="{{ route('logout') }}" class="mt-3">@csrf<button class="text-xs text-slate-500 hover:text-white">Sign out</button></form></section></main></body></html>
